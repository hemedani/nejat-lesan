# TODO — Mobile Patrol App (Backend)

Observable task list for the Deno/LESEN backend work required to support the new React Native patrol-officer app. Items are grouped by domain. Execution order lives in `CONTINUE.md`.

## 1. Authentication & Device Management

- [x] User model: add `personnel_code` (numeric-only), `is_active`, `patrol_permissions`, and the `"Patrol"` user level (Step 1)
- [x] New `device` model (device_id, fingerprint, platform, app_version, model, is_active, last_seen_at, registered_at, revoked_at) + `owner` → user relation (Step 1)
- [x] Wire `device` into `models/mod.ts` + `mod.ts`; unique sparse index on `personnel_code` (Step 1)
- [x] Unified `login` act — login with `email` + `password`; validates `is_active`; lockout policy; rejects non-patrol accounts when device payload is sent; registers/updates device; returns `{ token, user }` (web) or `{ token, user, permissions }` (mobile — devices via the `user.devices` reverse relation) (Step 2 + Step 11 unification; the old personnel-code-keyed `mobileLogin` act was removed)
- [x] Session/refresh-token strategy for long-lived patrol sessions (device-scoped token; token revocation on device revoke)
- [x] Admin acts: `getUserDevices` (list active devices per officer), `revokeDevice` (force logout by device_id), `removeDevice`
- [x] Failed-login lockout: track consecutive failures per user/device, apply time-based lockout, expose remaining lockout seconds
- [x] `getMe`/profile payload: include `patrol_permissions`, active shift, patrol unit, and vehicle when present

## 2. Offline-First & Synchronization

- [x] `client_report_uuid` (string, unique sparse index) on the accident model — idempotency key generated by the app (Step 6)
- [x] Idempotent `add` act: look up by `client_report_uuid` first; return the existing record (200) instead of duplicating; explicit conflict/success signals (Step 8)
- [x] Idempotent `update` act: update by `client_report_uuid` when provided (Step 8)
- [x] Sync status field on accident (`draft | queued | syncing | synced | rejected`) + `getSyncStatus` endpoint (per-status arrays; Patrol-scoped via `officer._id`) — E2E verified 2026-08-24
- [x] Announcements channel (`add`/`gets`/`get`/`markRead`/`getUnreadCount`) with real per-user read tracking via the `announcement_read` model (unique per user+announcement); `gets` annotates `is_read`/`read_at` and sorts unread-first — E2E verified 2026-08-24

## 3. Location, Road Mapping & Linear Referencing

- [x] Road model expansion: direction metadata (origin/destination), ordered geometry points for linear referencing, distance markers, lane/band definitions (Step 9)
- [x] `snapPointToRoad` act — map lat/lng → nearest road + direction + kilometer/meter (spatial query + geodesic projection) (Step 9)
- [x] `validatePointInZone` act — verify a point is inside the officer's valid patrol zone / road network boundary (Step 9)
- [x] Zone/road-boundary data for offline map caching (endpoint returning road geometry for a patrol area) (Step 9)

## 4. Reference Data (Phase 4–7 form values)

- [x] Wire existing `police_station.ts` model into registration (currently unregistered)
- [x] Create new shared models (7): `vehicle_type`, `croquis_type`, `vehicle_final_status`, `driver_status`, `injury_status`, `person_role`, `damage_severity`
- [x] Seed the new models with the values from `docs/app_fa.md`
- [x] Add missing seed values to existing models: `position` (خط ۱، خط ۲، شانه راست، شانه چپ), `road_situation` (قوس افقی، شیب/فراز، قوس قائم، محدوده عوارضی، عملیات راهسازی، شانه راه), `road_surface_condition` (مرطوب، خیس، آبگرفته), `road_defect` (آبگرفتگی، محدودیت دید، نقص گاردریل), `equipment_damage` (نیوجرسی، پایه تابلو/روشنایی، چراغ، فنس، دوربین، تجهیزات عوارضی، پل/آبرو)
- [x] Add basic acts (add/get/gets/update/remove) for the new models following the existing shared-model patterns

## 5. Operational Models (Patrol Unit & Shift)

- [x] `patrol_unit` model (code, name, police_station relation, vehicles, active officers)
- [x] `shift` model (patrol_unit, officer, vehicle, shift type, start/end, status) + acts: assign shift, `getActiveShift`, end shift, list officer shifts
- [x] Vehicle linkage: officer → active shift → patrol unit → vehicle; dashboard reads shift profile, not static data

## 6. Expanded Accident Model

- [x] Auto-meta block: `report_id` (auto-generated), `client_report_uuid`, officer relation, patrol_unit relation, vehicle relation, `reported_at`, `gps_coords` (officer location), `incident_coords` (selected map point), `gps_accuracy`, `travel_direction`, `kilometer`, `meter`, `lane` (relation to `position`) (Step 6)
- [x] Classification: `severity` (relation to `type`), expanded `collision_type` (already exists) (Step 7)
- [x] Police/Croquis block: `police_present`, `police_station` (relation), `police_expert_name`, `police_arrival_time`, `croquis_type` (relation), `officer_cause_description` (Step 7)
- [x] Vehicles array expansion: vehicle_type, brand (`system`), model (`system_type`), color, year, final_status, plate image + insurance images (file refs), driver block (name, phone, national id, licence, `driver_status`, insurance info) (Step 7)
- [x] New `people_dtos` array: role (`person_role`), demographics, injury status (`injury_status`) (Step 7)
- [x] Environmental block: weather (`air_status`), lighting (`light_status`), surface (`road_surface_condition`), geometry (`road_situation`), road defects (`road_defect`) (Step 7 — already existed)
- [x] New `facility_damage_dtos` array: asset group (`equipment_damage`), asset code, `damage_severity`, quantity/unit, creates hazard, needs repair, temporary action, damage images (Step 7)
- [x] File uploads: categorized multi-image uploads (plate, insurance, croquis, damage) linked to accident `attachments` — `file.uploadAccidentImages` accepts **base64 in `set.file.data`** (`{name,type,data}`) because the mobile transport is JSON-only; `damage` is accepted as an alias for `facility_damage`; croquis cap raised to 10; Patrol ownership of `accidentId` enforced — E2E verified 2026-08-24

## 7. Acts for Accident

- [x] `add` act: idempotency by `client_report_uuid`, auto `serial`/`report_id` generation, accept new meta/form fields (Step 8)
- [x] `update` act: support new fields + idempotent update by uuid; mark status `synced`/`rejected` per validation (Step 8)
- [x] `getMyReports` act: officer's own reports with status + rejection notes (Step 8)
- [x] `gets`/`get` projection updates for new fields (Step 8 — `selectStruct` auto-includes them)

## 8. Misc / Housekeeping

- [x] **Note:** Lesan `insertOne` does not apply `defaulted` defaults — set them explicitly in fn docs (discovered in Step 2)
- [x] Update `Models.md` for new models and expanded accident/user/road schemas (see `Models.md`; updated for `device`, `shift`, `patrol_unit`, `announcement`, `announcement_read`, `emergency`, `accident_review`)
- [x] Update `deno.json`/docs if needed; regenerate frontend types on server start (`declarations/selectInp.ts` is copied to `front/src/types/declarations/`)
- [x] Verify responses keep the `{ success, body }` envelope (framework-default) for all new acts — confirmed via E2E 2026-08-24

## 9. Handoff additions (2026-08-24)

- [x] `police_station` CRUD acts (`add/get/gets/update/remove/count`) — public `gets`, Manager-gated writes; mobile station picker can send `policeStationId`
- [x] Emergency/SOS: new `emergency` model + acts — `emergency.register` (Patrol; officer from token, unit/vehicle auto-resolved from active shift, GPS + connection status + timestamp), `gets`/`get`/`updateStatus` (Manager). Offline fallback (SMS/call) remains an ops policy decision
- [x] Patrol map overlay: `accident.nearbyAccidents` — lightweight `{location, report_id, seri, date_of_accident, review_status, type_name}` docs in a bounding box; synced reports only; Patrol requires `patrol_permissions.can_view_map`
- [x] Push-token contract: `device.push_token` stored at device-scoped login (`set.device.push_token`); FCM/APNs delivery integration still open (needs provider credentials)
- [x] Security hardening: `accident.add` server-enforces Patrol officer attribution (cannot create reports under another officer's id)
- [x] Fixed all pre-existing `deno check mod.ts` errors (12) — CI gate now clean

## 10. Model refactor pass (2026-08-25)

- [x] **`announcement_read`**: replaced the raw `announcement_id` pure field with a proper Lesan `announcement` single relation (unique index on `{"announcement._id", "reader._id"}`); `markRead`/`getUnreadCount`/`gets` query `"announcement._id"`. Kept as a model (not embedded) because a single announcement can be broadcast to many officers.
- [x] **`file.accident_id` removed**: the `accident` single relation already exists; `uploadAccidentImages` count-check and `removeOrphans` now filter on `"accident._id"` / `accident: {$exists: false}`.
- [x] **`accident_review` model eliminated** → embedded `accident.review_history` (`{action, reason?, action_at, reviewer:{_id, first_name, last_name} snapshot}`); `reviewReport`/`resubmitReport` `$push` atomically; `getReportReviewHistory` reads the embedded array.
- [x] **Dead models deleted**: `driver.ts`, `person.ts`, `country.ts`, `location_area.ts` (broken `axes` schemaName), `event_process.ts`.
- [x] **`road.province` reverse renamed** `axeses` → `roads`; `road.add` / `user.seed` updated.
- [x] `accident.officer` string kept as a legacy immutable snapshot (used by web dashboard regex search + legacy seed); the authoritative link is the `officer` relation.
- [x] AGENTS.md expanded with: Complete Relation Maps, codebase one-directional examples, Prefer-Embedding + Prefer-Relations conventions, hardCascade bottom line, dead-code convention. `Models.md` synced.