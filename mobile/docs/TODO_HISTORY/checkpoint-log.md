# Checkpoint Log — Mobile Patrol App

Historical record of each checkpoint run. Kept in `TODO_HISTORY` so `CONTINUE.md` stays a single-page next-task prompt. Source: the checkpoint log previously embedded in `CONTINUE.md`.

| Date | Checkpoint | Result | Notes / blockers |
| --- | --- | --- | --- |
| 2026-08-21 | Documentation baseline | Done | Created `mobile/docs/TODO.md` and `mobile/docs/CONTINUE.md`; mobile implementation is still at scaffold stage. |
| 2026-08-21 | Checkpoint 0 | Partial | SDK 57 docs reviewed. With fnm Node `v22.22.2` / pnpm `v10.8.0`, TypeScript and Expo config pass; lint reaches the existing starter `use-color-scheme.web.ts` hook error. No emulator/device launch performed. |
| 2026-08-22 | Auth and Ghost access | Partial | Unified `user.login` permits device-scoped login for `Patrol` and `Ghost`; known auth messages are mapped, and the incident route is guarded. Mobile TypeScript and Android export pass; backend Deno check retains the repository's existing broad errors, and no credentialed Ghost E2E was run. |
| 2026-08-23 | Home request-storm fix | Done | Root cause: the Home effect re-ran on every restore because `sessionService.restore()` returns a fresh object identity, looping `shift.getActiveShift` until connections saturated (canceled fetches / truncated JSON). Extracted `use-active-shift` hook with one-time restore, in-flight guard, 60s auto-refresh cooldown, offline preflight, and a dedicated `no_active_shift` operational state for «شیفت فعالی یافت نشد». Hardened `client.ts` to classify timeout vs cancelled aborts vs truncated responses (`cancelled`, `invalid_response`) instead of raw SyntaxError/cancel noise; added Persian translations. Fixed the starter `use-color-scheme.web.ts` lint error with `useSyncExternalStore`. `pnpm exec tsc --noEmit` and `pnpm lint` both pass. Device verification pending on user's Expo Go phone. |
| 2026-08-23 | Five-tab shell and OSM map tab | Partial | Installed SDK 57-aligned `expo-location@57.0.12`, `react-native-maps@1.27.2`, `@expo/vector-icons@15.1.1`. Restructured routes into `src/app/(tabs)` (Reports/Announcements/Home-center/Map/More) with a custom RTL tab bar; login moved to `/login`; stale starter routes removed. Home gained an on-demand GPS status pill. Map tab renders OSM-only tiles via `UrlTile` (no satellite mode anywhere) with officer marker and attribution; tiles are online-only for now — offline caching is Checkpoint 5 work. More tab has functional logout. Verified: `tsc --noEmit`, `pnpm lint`, and `expo install --check` all pass. Device verification on Expo Go pending; protected-route guarding per tab is still coarse (index bounce). |
| 2026-08-23 | Draft engine and sync queue | Partial | Added pure `sync-rules` (backoff 1/5/15/60 min, max 5 attempts, stale-syncing recovery after 10 min, outcome decision by error code), `accident-mapper` (draft → GeoJSON Point + ISO date + passthrough DTOs; local guard when no coordinates), typed `accident.add`/`update` wrappers with `{ _id, report_id }` ack projection, and a single-flight sync worker triggered on start/connectivity with manual run support. New `/drafts` screen shows status chips, attempts, next retry, errors, per-item resend, and sync-all; incident screen now reuses one empty draft instead of creating duplicates; drafts entry point added to Home. Backend contract confirmed: Patrol may only set `draft|queued`, idempotency via `client_report_uuid`. Verified `tsc --noEmit`, `pnpm lint`, `expo install --check`. Device testing of real submission/rejection flows pending until the accident form phases exist. |
| 2026-08-23 | Incident location picker | Partial | New `/incident/location` screen (incident route moved to folder) with OSM-only tiles, fixed teal center pin over pannable map, distinct blue officer marker, recenter control, and a summary sheet (selected coords, officer distance with >2 km warning, GPS accuracy with >50 m warning). Debounced+aborted `road.snapPointToRoad` fills road/direction/km+m/distance rows; offline degrades to a notice. Confirm runs best-effort `road.validatePointInZone` (non-blocking warning stored in `draft.data.zone_check`) and saves `gps_coords`/`incident_coords`/`road_snap`/`gps_unavailable` via `saveIncidentLocation`; Edit Location restores the previous pin. Incident screen shows the saved location summary and zone warnings. Verified `tsc --noEmit` + `pnpm lint`; device testing pending (production roads still lack `area` geometry, so snapping may return «نزدیک‌ترین راه در شعاع پوشش یافت نشد»). |
| 2026-08-23 | Blank map + stale draft fix | Done | Device report: map rendered blank and the incident screen kept showing «هنوز موقعیتی انتخاب نشده است» after confirming. Fixes: (1) replaced `react-native-maps` (Google base dependency; blank under GMS restrictions) with `OsmWebMap`, an in-house WebView slippy-map rendering OSM tiles directly — pan, double-tap zoom, zoom buttons, officer marker bridge, moveend callbacks; removed the `react-native-maps` dependency; Map tab and picker both use it. (2) incident draft now reloads via `useFocusEffect` on return from the picker, confirm is disabled without a draft uuid, and the selected point derives from officer coords until the user pans (no effect-setState). Verified `tsc --noEmit` + `pnpm lint`. |
| 2026-08-23 | Smooth panning + snap auth | Done | Two device reports fixed: (1) panning was janky because every animation frame rebuilt all tile `<img>` nodes via innerHTML; OsmWebMap now keeps persistent tile elements positioned in world pixels and moves the whole layer with a GPU translate3d transform, reconciling (add/remove only changed tiles) against a 2-tile buffer throttled to 140 ms — DOM churn during pan is eliminated. (2) `road.snapPointToRoad` was called without the auth header («you should send your id with token key in req header»); the picker now passes the session token. Verified `tsc --noEmit` + `pnpm lint`. |
| 2026-08-23 | White-map regression fix | Done | The world-pixel refactor broke rendering: Chromium clamps layout coordinates at ~2^24 px, and at zoom 15-17 tile `left/top` values exceed that, so every tile collapsed to nothing (white canvas) even though the JS bridge still worked. OsmWebMap now keeps tiles at CSS origin 0/0 and positions each one per frame with small screen-local `translate3d` transforms (persistent nodes → no DOM churn; no oversized layout coords). Also re-sends the officer-marker payload on WebView load end (guards lost pre-load messages), dedupes identical consecutive `[LESAN API]` dev warnings, and gives «نزدیک‌ترین راه در شعاع پوشش یافت نشد» its own hint («راهی در شعاع پوشش این نقطه ثبت نشده است»). Verified `tsc --noEmit` + `pnpm lint`. |
| 2026-08-23 | Pinch-to-zoom | Done | Camera model upgraded to fractional zoom (`zoomF` float): tiles render from `floor(zoomF)` scaled by `2^(zoomF-floor)` via GPU transforms, swapping tile sets when the integer level changes mid-gesture. Two-finger pinch scales zoom by `log2(distance ratio)` while keeping the originally grabbed geographic point under the moving midpoint; releasing one finger hands off smoothly to single-finger pan. Manual double-tap zoom added (system dblclick is suppressed by the preventDefault touch handlers); zoom buttons unchanged. Confirmed the remaining `[LESAN API]` warnings are expected: «نزدیک‌ترین راه…» is the production road-geometry backend blocker and «cancelled» is the debounce aborting superseded requests. Verified `tsc --noEmit` + `pnpm lint`. |
| 2026-08-23 | Smooth animated transitions | Done | Added a rAF animation engine to OsmWebMap with easeOutCubic tweens: double-tap and +/- buttons animate zoom over 240 ms while keeping the anchor geographic point fixed under its screen position; `setCenter` (recenter button, GPS auto-center, future callers) animates pan+zoom together over 320 ms. Any touch immediately cancels the running animation (user takes control), tiles keep reconciling during flight via the existing throttled path, and `moveend` (→ road snap) fires once on settle instead of per frame. Verified `tsc --noEmit` + `pnpm lint`. |
| 2026-08-23 | Anchored-zoom jump fix | Done | Double-tap (and +/- buttons) flung the camera to a far-away location: `setZoom` captured the anchor as world-pixels at the old zoom but re-interpreted them at the new zoom without multiplying by `2^(Δzoom)`. The anchor is now rescaled explicitly, so the tapped point stays fixed under the finger while zooming. Pinch was unaffected (it anchors on stored lat/lng). Verified `tsc --noEmit` + `pnpm lint`. |
| 2026-08-23 | Accident form phases 1-3 | Partial | New `/incident/details` screen with a 3-phase stepper (اطلاعات پایه / طبقه‌بندی تصادف / پلیس و کروکی), progress bar, chip-based pickers, conditional police block, and pure per-phase Persian validation (`domain/accident-form.ts`). References load through cache-first `type.gets`/`collision_type.gets`/`croquis_type.gets` into SQLite with stale-on-offline fallback; autosave debounces 500 ms to SQLite and flushes on every transition/back; incident screen gained a «تکمیل اطلاعات گزارش» CTA once location is confirmed. Backend gaps recorded: no severity field on accident schema (severity stored locally only) and no `police_station.gets` act (station captured as free text). Mapper now forwards `collisionTypeId`. Verified `tsc --noEmit` + `pnpm lint`; device testing pending. |
| 2026-08-23 | Accident form phases 4-7 | Partial | Form expanded to all seven phases. Vehicle cards (type/brand/type-model/color/year/final status/plaque type+3-part number+usage/fault/motion direction/third-party & body insurance/driver block with licence and injury refs), people sections (passengers/pedestrians/generic people with role+age), environment (air multi, light, road situation, surface multi, defects multi → root array relations via mapper `ARRAY_RELATION_KEYS`), and facility damage cards (asset group/code, severity, quantity/unit, hazard/repair flags, temporary action; images pending). All 27 reference lists load through the generic cache-first `loadReferenceSet`. The sync worker blocks submission of drafts whose required vehicle-card fields are incomplete — draft stays queued with an explanatory hint and no attempt penalty. Data persists in backend-shaped DTO keys so the idempotent payload needs no transformation. Verified `tsc --noEmit`, `pnpm lint`, `expo install --check`; device testing pending. |
| 2026-08-23 | Media local layer (Checkpoint 9) | Partial | Installed SDK 57-aligned `expo-image-picker@57.0.12`, `expo-image-manipulator@57.0.12`, `expo-file-system@57.0.5`. New `media-service.ts`: camera/library capture with on-demand Persian-explained permissions, resize ≤1600 px + JPEG q0.7 compression, copy into `Documents/media/<uuid>/` via the new File/Directory API (survives cache clears), SQLite records with category + owner-tag metadata, atomic replace (old file deleted) and delete. Reusable `MediaSection` component (thumbnails, tap-to-preview modal, replace/delete) wired into vehicle cards (plate/insurance), police phase (croquis), and facility cards (damage). Picker permission strings added to app.json plugin. Upload/reconciliation intentionally deferred: backend Step 11 categorized-upload contract is still open — documented as blocked in Checkpoint 9. Verified `tsc --noEmit` + `pnpm lint` + `expo install --check`; camera/gallery behavior pending on-device test. |
| 2026-08-23 | Reports tab real data + More enrichment (Checkpoint 10) | Partial | Reports tab replaced placeholder with live `accident.getMyReports` list (Patrol-scoped contract verified): dual status chips for sync + review lifecycle, rejection notes highlighted, «اصلاح گزارش» routes returned/rejected reports back to their matching local draft, pull-to-refresh + retry/offline states. More tab now shows profile fields, live shift/unit/vehicle from `shift.getActiveShift` with no-shift and retry states, device id, app version, functional logout, and به‌زودی rows for PIN/biometric, app-lock, notifications, offline map packages, guide/support (each tied to a documented dependency). Announcements remain placeholder (backend channel open); map incidents/roads need backend geometry + patrol map act. Verified `tsc --noEmit`, `pnpm lint`, `expo install --check`. |
| 2026-08-23 | Smooth animated transitions | Done | Added a rAF animation engine to OsmWebMap with easeOutCubic tweens: double-tap and +/- buttons animate zoom over 240 ms while keeping the anchor geographic point fixed under its screen position; `setCenter` (recenter button, GPS auto-center, future callers) animates pan+zoom together over 320 ms. Any touch immediately cancels the running animation (user takes control), tiles keep reconciling during flight via the existing throttled path, and `moveend` (→ road snap) fires once on settle instead of per frame. Verified `tsc --noEmit` + `pnpm lint`. |
| 2026-08-25 | Backend contract integration + Checkpoint 12 start | Partial | Consumed the now-real backend acts from the updated handoff. (1) Correction loop closed: sync worker picks `accident.update` by uuid when `draft.server_id` exists (`pickSubmissionAction`), chains `accident.resubmitReport(set.reportId)` for drafts flagged «returned» (definitive rejections clear the flag, transport errors retry), «اصلاح گزارش» marks the draft returned with the expert note, edited synced drafts requeue on form exit plus a run-start reconciliation pass. (2) Severity → backend `type` relation: classification picker drives `typeId` from loaded خسارتی/جرحی/فوتی references with local fallback offline; validation requires typeId only while refs are loaded; mapper never leaks local severity. (3) Announcements tab live against `announcement.gets`/`markRead`/per-user read state with priority badges, expiry, optimistic mark-read + revert, unread badge. (4) Media upload pipeline coded dormant behind `EXPO_PUBLIC_ACCIDENT_UPLOADS=on`: typed base64-JSON act wrapper, idempotent per-file upload service, pure dto write-back (`plate_image`/`insurance_image`/facility `images`). (5) More tab refreshes through `user.getMe` incl. active-device count. (6) Vitest added (`pnpm test`, dev-only): 45 tests across validators, mapping, sync rules/correction decisions, envelope/errors, write-back, UUID preservation. SDK 57 patch drift synced via `expo install --fix`. Verified `pnpm exec tsc --noEmit`, `pnpm lint`, `expo install --check`, `pnpm test` (45/45). On-device verification of the correction loop, announcements, and upload enablement remains the next priority. |
| 2026-08-25 | GPS recovery + route-warning fixes | Done | Device reports: (1) Expo Router warned that `app/incident/details-phases.tsx` has no default export — the shared phase components moved out of `src/app` to `src/components/incident-phases.tsx` so only real routes live under `app/`. (2) «موقعیت مأمور در دسترس نیست» persisted after enabling GPS: `useDeviceLocation.locate()` ran once per mount and never recovered. The hook now keeps the last known fix on failed retries, re-prompts permissions when allowed, polls `hasServicesEnabledAsync` every 2.5 s while services are off (auto-recovers the moment GPS is enabled), and exposes a smart `recoveryAction`: «روشن کردن GPS» opens Android location settings (`Linking.sendIntent('android.settings.LOCATION_SOURCE_SETTINGS')`; SDK 57 expo-location has no `enableServicesAsync`) / iOS app settings, «اجازه دسترسی به موقعیت» re-requests permission, otherwise retry. New shared `GpsActionButton` wired into the incident location sheet, the Map tab status card (which now also locates on mount — the officer marker previously never appeared), and the Home GPS pill (dynamic label: روشن‌کردن GPS / مجوز / تلاش). The remaining `[LESAN API] … نزدیک‌ترین راه در شعاع پوشش یافت نشد` dev warning is the documented backend blocker (~1490 production roads lack `area` geometry); the picker already degrades to a non-blocking hint. Verified `pnpm exec tsc --noEmit`, `pnpm lint`, `pnpm test` (48/48). |
| 2026-08-25 | Offline map for Iran (Android) | Partial | Implemented the offline-tile pipeline end to end. Domain: pure `map-packs.ts` (Iran bbox, base z4–10 / deep z4–12 tiers, XYZ iterator, estimators — 9 new unit tests; real sizing confirmed: base ≈4.1k tiles ≈ 60–120MB, deep ≈66.5k ≈ 1.2GB est). Storage: SQLite migration v2 (`map_packs`, `map_tiles` empty-negative-cache, `app_settings`). Engine `services/map-download.ts`: single national pack, concurrency-4 worker pool over an async pending-coords iterator, temp+move atomic writes, skip-existing resume, per-tile retries (1s/5s/15s), consecutive-failure abort → failed, throttled counter flushes, filesystem recount on run start, offline/cellular auto-pause with Wi-Fi-only setting default-on, watcher auto-resumes only system-set pauses (manual «توقف» stays). Tile store mirrors `Documents/mapcache/{z}/{x}/{y}.png`. Rendering: map HTML now hosted as a real `file://` document (`services/map-web-html.ts`, versioned file + stale cleanup) loaded with Android file-access flags; tiles resolve local-first with per-tile network fallback; initial camera delivered via a new `setView` bridge message (HTML is screen-independent). UI: Home «نقشه آفلاین» card (download %, pause, manage), `/map-offline` management screen (tier cards, upgrade-to-کامل confirmation, Wi-Fi-only switch, delete flow, storage + attribution notes), More-tab row now routes there. Verified `tsc --noEmit`, `pnpm lint`, `pnpm test` (57/57). Pending on-device: airplane-mode panning across Iran, kill/resume mid-download, upgrade path, and cellular auto-pause behavior. |
| 2026-08-28 | Police station picker (CONTINUE §4) | Done | `police_station` added to the cache-first reference set (`RefModel` + `NEEDED_REFERENCE_MODELS`); PhasePolice replaced the manual station text with a ListRow picker from `police_station.gets` sending `policeStationId` (station name kept as display/fallback), retaining the manual free-text input while seed data is missing. Form state round-trips `policeStationId`/`police_station_name`; mapper test confirms the station goes out as a relation id, never as free text. `pnpm exec tsc --noEmit`, `pnpm lint`, `pnpm test` (59/59) clean. Remaining CONTINUE tasks (on-device map/redesign/upload E2E) need a real device + reachable backend. |
